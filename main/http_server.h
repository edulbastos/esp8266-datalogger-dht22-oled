#ifndef HTTP_SERVER_H
#define HTTP_SERVER_H

/**
 * @brief Tarefa do servidor HTTP
 * @param pvParameters Parâmetros da task (não utilizado)
 */
void http_server_task(void *pvParameters);

#endif // HTTP_SERVER_H
